* the tab context menu should appear at where the cursor is.
* the search field in the coruse node works for searching a course, but the user cannot click on the course to confim the selection.
* the node port (the small dot circle) loation is still wrong, they are much lower than their corresponding labels.
