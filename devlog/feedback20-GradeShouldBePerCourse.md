Add another tab called low grade reporter to next to the Student Course Audit tab.

this tab should allow the user to put in a filter to filter out a collection of courses, it has the same behavior of the Course Collection node in the Aggregation graph:

It has filters like:

* Term
* Department
* Course Number (support wild card)

but it will also have one more filed called exlcude

And it will filter out a list of courses that fulfils the filter.

It should also have a minimum grade field for the user to define a percent, defaults to 70%.





